# Release 64 check-in

Changes since `3d06c62c` (public release 63). Suggested commit message:

```text
Fix GDLE packet timing and release client v64

- Replace CPU-cycle packet timestamps with retail half-second intervals
- Share the connection clock across new and retransmitted packets
- Reset timing on reconnect and preserve 16-bit interval wraparound
- Apply the correction to desktop, PC VR, and standalone Quest
- Test use, move, and equip packets against GDLE payloads and timing checks
- Verify inventory replies preserve the in-world session
- Release Windows 2026.09.22.70 and Quest 2026.09.22.quest.64
```

The old timing field could trigger GDLE's speed-hack disconnect check. This
corrects a confirmed protocol error found during the Seedsow investigation;
live Seedsow confirmation is still pending.

Development and packaged Windows suites passed: GDLEInteractionTransport,
LoginHandshake, NetworkTransport, and LinkTiming. Item coverage captures 480
use/move/equip packets over loopback,
checks checksums and retransmissions, and validates successful inventory replies.

Windows and Android packaging succeeded. Both public landing pages now link to
v64. All 14 allowlisted files verified over FTPS; the three complete downloads,
both pages, release metadata, notes, and checksums matched staging over HTTPS.

Release files: `C:/dev/ACE/Releases/2026.09.22-v64`.
Public verification: `Unreal/Saved/GDLECompatibility/public-verification64.json`.
Deployment backup: `C:/dev/Thwargle.com/_tools/deploy-backups/20260922-160019`.

The local headset was not updated as part of this publication. Live Seedsow
and headset acceptance remain outstanding; no new FPS measurement is claimed.

Builds, installer output, and deployment evidence remain in ignored directories.
No source changes are committed automatically.
