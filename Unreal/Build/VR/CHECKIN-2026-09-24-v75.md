# Release v75 check-in notes

```text
Release v75: stabilize the VR compass and add panel placement controls

- Give the compass Head, stabilized Body, and World anchors like native vitals.
- Smooth compass heading independently of the bounded 10 Hz radar scan.
- Rotate compass labels, cached markers, and selection hit targets together.
- Add controller Move, Lock/Unlock, and Resize handles to compass, vitals,
  main VR menu, and VR options; preserve center and aspect ratio when resizing.
- Support moving head-pinned menus and persist HUD placement/size preferences.
- Add compass/options size sliders and independent placement locks.
- Stop layout edits on release, hiding, tracking loss, and recentering.
- Keep hidden handles inactive and preserve combat input outside the UI.
- Add regression coverage for anchoring, locomotion, controller handles,
  resize stability, input release, heading wrap, and invalid saved settings.
- Advance Windows to 2026.09.24.81 and Quest to 2026.09.24.quest.75.
- Publish v75 downloads and updater metadata for desktop, PC VR, and Quest.
```

Validation: Windows/Quest packaging passed. Six packaged regression tests passed
(five clean, one with an existing warning), including VR, login UI, and updater
integrity. The real v74 -> v75 portable installer preserves DAT, account, and
settings fixtures. Quest APK signature matches v74. Updater helper tests pass
for desktop/VR restart, quoted paths, and corrupt-payload rejection.

Public release notes: https://thwargle.com/downloads/ac/v75/RELEASE-NOTES.md

Headset comfort/perceived smoothness still needs a worn-headset check. No measured
FPS gain is claimed. Generated material asset churn was removed from this diff.

Publication verified: all 15 public HTTPS files match local SHA-256. Both download
pages and the schema-1 updater manifest advertise v75; the manifest returns HTTP
200 without a redirect and references the verified installer and direct APK.
